# SafeQuery AI

SafeQuery AI is a secure natural-language-to-SQL analytics platform.

It converts business questions into PostgreSQL queries, validates them
through configurable safety guardrails, executes them using read-only
database permissions, detects possible hallucinations, and returns an
explainable confidence score.

## Current status

Milestone 1: Project initialization and FastAPI health endpoint.